<% page_title t('subject_portfolios.assignment.title') %>

<div class="container py-3">
  <h1><%= t('subject_portfolios.assignment.title') %></h1>
  <hr class="primary">
  <%= render 'form', remote: false, modal: false %>
</div>
