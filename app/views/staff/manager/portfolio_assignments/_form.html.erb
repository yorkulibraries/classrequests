<% remote_form = local_assigns.fetch(:remote, false) %>
<% modal_form = local_assigns.fetch(:modal, false) %>

<%= simple_form_for(
      @teaching_request,
      url: staff_manager_portfolio_assignment_path(id: @teaching_request.id),
      method: :patch,
      remote: remote_form,
      html: { id: 'portfolio_assignment_form' }
    ) do |f| %>
  <%= f.error_notification %>
  <%= f.error_notification message: f.object.errors[:base].to_sentence if f.object.errors[:base].present? %>

  <section aria-labelledby="portfolio-request-information-heading">
    <h2 id="portfolio-request-information-heading" class="h5 text-primary">
      <%= t('subject_portfolios.assignment.request_information') %>
    </h2>
    <dl class="row small mb-4">
      <dt class="col-sm-3"><%= t('subject_portfolios.assignment.fields.requestor') %></dt>
      <dd class="col-sm-9"><%= @teaching_request.first_name %> <%= @teaching_request.last_name %></dd>

      <dt class="col-sm-3"><%= t('subject_portfolios.assignment.fields.email') %></dt>
      <dd class="col-sm-9"><%= mail_to @teaching_request.email %></dd>

      <dt class="col-sm-3"><%= t('subject_portfolios.assignment.fields.course') %></dt>
      <dd class="col-sm-9 mb-0">
        <%= @teaching_request.faculty_abbrev %>/<%= @teaching_request.subject_abbrev %>
        <%= @teaching_request.course_number %> <%= @teaching_request.academic_year %>
      </dd>
    </dl>
  </section>

  <section aria-labelledby="portfolio-assignment-heading">
    <h2 id="portfolio-assignment-heading" class="h5 text-primary">
      <%= t('subject_portfolios.assignment.heading') %>
    </h2>
    <%= f.input :subject_portfolio_id,
                collection: @subject_portfolios,
                label: t('subject_portfolios.assignment.fields.subject_portfolio'),
                label_method: :name,
                value_method: :id,
                prompt: t('subject_portfolios.assignment.prompt'),
                required: true,
                input_html: { class: 'form-select' } %>
  </section>

  <div class="d-flex gap-2 border-top pt-3 mt-4">
    <%= f.button :submit,
                 t('subject_portfolios.assignment.actions.assign'),
                 class: 'btn btn-success',
                 disabled: !@teaching_request.portfolio_assignable? %>
    <% if modal_form %>
      <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">
        <%= t('subject_portfolios.assignment.actions.cancel') %>
      </button>
    <% else %>
      <%= link_to t('subject_portfolios.assignment.actions.cancel'),
                  staff_manager_teaching_requests_path,
                  class: 'btn btn-secondary' %>
    <% end %>
  </div>
<% end %>
